[project]
name = "neural"
version = "1.0.0"
description = "Neural"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "ipython>=8.38.0",
    "matplotlib>=3.10.8",
    "numpy>=2.2.6",
]
